import { FaqProps } from './types';
declare const C002: {
    ({ title, queries }: FaqProps): import("react/jsx-runtime").JSX.Element;
    propRequire: {
        queries: (number | {
            title: string;
            description: string;
        })[];
        title: string;
        description: string;
        contactLink: string;
    };
};
export default C002;
