import { HeaderProps } from "./types";
declare const C002: {
    ({ features, logoSrc }: HeaderProps): import("react/jsx-runtime").JSX.Element;
    propRequire: {
        features: (number | {
            title: string;
            href: string;
        })[];
        logoSrc: string;
    };
};
export default C002;
