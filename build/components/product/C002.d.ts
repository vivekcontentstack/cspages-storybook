import { ProductProps } from './types';
declare const C002: {
    ({}: ProductProps): import("react/jsx-runtime").JSX.Element;
    propRequire: {
        shortTitle: string;
        title: string;
        description: string;
        items: (number | {
            title: string;
            subtitle: string;
        })[];
    };
};
export default C002;
